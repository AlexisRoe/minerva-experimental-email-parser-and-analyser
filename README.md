# minerva-email-parser

Test lab for parsing and analysing emails in Python without the overhead of the
Minerva application.

## Features

- Bullet list of what it does — fill in as the project grows.

## Getting started

Requirements: [`uv`](https://docs.astral.sh/uv/).

```bash
git clone <repo-url>
cd minerva-experimental-email-parser-and-analyser
make install
```

Other useful targets:

```bash
make test     # run the test suite
make lint     # ruff check + format check
make format   # ruff check --fix + format
make check    # test + lint
```

## Architecture

<!-- One paragraph + a short diagram/bullet list of the module layout. -->

## Technologies used

| Layer               | Technology |
| ------------------- | ---------- |
| Language / runtime  | Python 3.12 |
| Package management  | `uv`       |
| Testing             | `pytest`   |
| Linting/formatting  | `ruff`     |

## Contributing

Please make sure `make check` passes before requesting review.

## License

See [`LICENSE`](./LICENSE).
